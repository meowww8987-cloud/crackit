# Fix peer dependency conflicts during npm install on Vercel/Netlify/etc.
# The @hookform/resolvers + valibot version mismatch causes ERESOLVE errors.
# legacy-peer-deps tells npm to ignore peer dep warnings (same as --legacy-peer-deps flag).
legacy-peer-deps=true

# Use exact versions for reproducible builds
save-exact=false
